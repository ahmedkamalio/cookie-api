language: node_js

node_js:
  - 'node'
  - '10'
  - '8'

env:
  - workerCount=3 timeout=600000

matrix:
  fast_finish: true

branches:
  only:
    - master

install:
  - npm set progress=false
  - npm install

cache:
  directories:
    - node_modules

script:
  - npm run test

git:
  depth: 1
